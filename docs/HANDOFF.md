# 引き継ぎ（2026-08-06）

『放課後トークルーム』公開作業の引き継ぎ。**何が終わっていて、何が残っていて、なぜ残ったか。**

---

## 役割

| 担当 | できること | できないこと |
| --- | --- | --- |
| あっきー | 最終判断・Drive操作・GitHubの管理者操作・YouTube操作・ログインが要る全部 | — |
| Claude（リモート実行環境） | 文書・スクリプト・リポジトリ構成・記録の整備・GitHub API経由の読み書き | 下記「できなかったこと」参照 |

**パスワードはチャットで渡さないでください。** ログイン画面が出る作業は、すべてあっきーが自分で操作します。

---

## 終わっていること ✅

| | 内容 |
| --- | --- |
| ✅ | Drive バックアップフォルダの中身を確認（13ファイル／欠品なし） |
| ✅ | `ORIGINAL_SHA256.txt` `PARTS_SHA256.txt` `restore-windows.bat` `restore-mac-linux.sh` `README_復元方法.txt` を取得し、内容を検証 |
| ✅ | **8パーツの合計サイズ = 679,926,904 バイト**（648.4 MiB）が、原本の想定サイズ「約649MiB」と一致することを確認 |
| ✅ | 復元スクリプトが **part-00→07 の正しい順で単純バイナリ連結**しており、再エンコードを含まないことを確認 |
| ✅ | 復元＋照合を自動化する `scripts/restore-and-verify.sh` を作成。**正常系／欠品／破損の3経路を実際に動かして検証済み** |
| ✅ | 次作用の `scripts/split-for-backup.sh` を作成。**分割→復元でバイト単位完全一致することを実測で確認** |
| ✅ | このリポジトリ一式（README・利用条件・手順書・制作記録・テンプレート）を作成 |
| ✅ | YouTube投稿文・Studio設定を Drive の資料から取り込み、`tracks/after-school-talk-room/YOUTUBE.md` を正本として整備 |

---

## できなかったこと ❌ と、その理由

### 1. 高画質版そのものの復元・SHA-256照合

**理由：この実行環境から Google Drive にネットワーク接続できません。**

Claude が動いているのはクラウド上の隔離コンテナで、通信は許可リスト方式のプロキシを通ります。
実測した結果：

| 接続先 | 結果 |
| --- | --- |
| `drive.google.com` | ❌ 403（ポリシーで遮断） |
| `drive.usercontent.google.com` | ❌ 到達不可 |
| `docs.google.com` | ❌ 到達不可 |
| `www.googleapis.com` | ⭕ 到達するが、APIキーがないため 403 |
| `github.com` | ⭕ 到達可 |

Drive連携（MCP）経由なら**ファイルの中身は読めます**が、返ってくるのは会話に載る base64 テキストです。
90MiB のパーツ1個で約1億2600万文字になり、8個ぶんは現実的に扱えません。
実際、小さなテキストファイル（150〜1376バイト）はこの方法で取得し、上の検証に使いました。

**つまり「読めるが、大きすぎて運べない」状態**です。回避策として再エンコードで小さくする手はありますが、
それは今回いちばんやってはいけないことなので採用していません。

→ **あっきーのPCで [`RESTORE.md`](RESTORE.md) を実行してください。** 5分の作業です。

### 2. スマホでタップできる高画質MP4のURL

**理由：上の1で元ファイルが手元に来ないため、どこにもアップロードできません。**

CloudFront / S3 の認証情報もこの環境にはありません。

→ **代替案があります。** GitHub Release のアセットURLが、そのまま要件を満たします。

- 再エンコードなし（アップロードしたバイト列がそのまま返る）
- 認証不要でスマホからタップして再生・保存できる
- CDN配信

わざわざ CloudFront を立てる必要はありません。詳細は [`RELEASE.md`](RELEASE.md) の「スマホから使えるURL」。

### 3. GitHubリポジトリ `manabi-ai-lab/manabi-bgm` の作成

**理由：この接続に使っている GitHub App に、Organization へのリポジトリ作成権限がありません。**

```
POST /orgs/manabi-ai-lab/repos
=> 403 Resource not accessible by integration
```

同名リポジトリが既に存在しないことは確認済みです（org内は `ganjin` `lab-private`
`manabi-ai-lab.github.io` `BGM` `lab-notes` `-song` の6つ。`manabi-bgm` はありません）。

→ **あっきーが空のPublicリポジトリを1つ作ってください。** [`STATUS.md`](../STATUS.md) に手順があります。

なお、このリポジトリの中身は消えていません。作業内容は
`manabi-ai-lab.github.io` の `claude/manabi-bgm-youtube-release-dkojhi` ブランチの
`manabi-bgm/` 配下に**push済み**です。そこから移すだけで使えます。

### 4. GitHub Release の作成

**理由：(a) 添付する649MiBのファイルが手元にない。(b) この環境のGitHubツールにRelease作成機能がない。**

→ `scripts/create-release.sh` を用意しました。**アップロード前にSHA-256を照合し、一致しなければ中止します。**

### 5. YouTube への投稿

**理由：YouTube はネットワークポリシーで遮断されており、そもそもログインが必要です。**

→ 投稿文・設定はすべて `tracks/after-school-talk-room/YOUTUBE.md` に確定済み。
Studio に貼るだけの状態にしてあります。

### 6. サムネイル画像のリポジトリ配置

**理由：1と同じ。** 265,782バイトの画像も、base64で会話に載せて書き戻すと転記事故のリスクがあります。
**画像は1バイトでも狂うと壊れる**ので、確実な方法を選びました。

→ `assets/thumbnails/README.md` に置き方を書いてあります。Driveから落として置くだけです。

---

## メモ：Drive の共有設定

バックアップフォルダは「リンクを知っている全員」が編集者（`writer`）です。
受け渡しの都合による**意図した設定**とあっきーに確認済み。変更しません。

---

## あっきーがやること（順番）

1. ✅ ~~空のPublicリポジトリ `manabi-bgm` を作る~~（2026-08-06 完了）
2. ✅ ~~中身を push する~~（2026-08-06 Claude が実行）
3. **高画質版を復元して照合** → [`RESTORE.md`](RESTORE.md)
4. **Release を作る** → [`RELEASE.md`](RELEASE.md)
5. **YouTube に限定公開で投稿** → [`YOUTUBE.md`](YOUTUBE.md)
6. **確認して一般公開**
